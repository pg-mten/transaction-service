name: Deploy transaction Service

on:
  push:
    branches:
      - release/development

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup SSH
        run: |
          mkdir -p ~/.ssh
          echo "${{ secrets.BIZNET_SSH_PRIVATE_KEY }}" > ~/.ssh/github_actions
          chmod 600 ~/.ssh/github_actions
          ssh-keyscan -H ${{ secrets.BIZNET_SSH_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy with Rollback
        run: |
          ssh -i ~/.ssh/github_actions \
          ${{ secrets.BIZNET_SSH_USER }}@${{ secrets.BIZNET_SSH_HOST }} << 'EOF'
            set -e

            cd ${{ secrets.BIZNET_DEPLOY_PATH }}

            echo "📦 Backup old image"
            docker tag backend-transaction:latest backend-transaction:rollback || true

            echo "⬇️ Pull code"
            cd /home/user-manapay/backend/transaction-service
            git checkout release/development
            git pull origin release/development
            cd ..

            echo "🚀 Deploy transaction"
            docker compose build transaction
            docker compose up -d transaction

            echo "⏳ Health check"
            sleep

            if [ "$(docker inspect --format='{{.State.Health.Status}}' transaction)" != "healthy" ]; then
              echo "❌ Transaction failed, rolling back"
              docker compose stop transaction
              docker rm transaction
              docker tag backend-transaction:rollback backend-transaction:latest
              docker compose up -d transaction
              exit 1
            fi

            echo "✅ Transaction deployed successfully"
          EOF
